version: '3.8'

services:
  rinhabackend:
    image: ${DOCKER_REGISTRY-}rinhabackend:latest
    container_name: rinhabackend
    depends_on:
      - db
    networks:
      - api_network
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=https://+:443;http://+:80
    ports:
      - "80"
      - "443"
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/root/.microsoft/usersecrets:ro
      - ${APPDATA}/ASP.NET/Https:/root/.aspnet/https:ro

  rinhabackend2:
    image: ${DOCKER_REGISTRY-}rinhabackend:latest
    container_name: rinhabackend2
    depends_on:
      - db
      - rinhabackend
    networks:
      - api_network
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=https://+:443;http://+:80
    ports:
      - "80"
      - "443"
    volumes:
      - ${APPDATA}/Microsoft/UserSecrets:/root/.microsoft/usersecrets:ro
      - ${APPDATA}/ASP.NET/Https:/root/.aspnet/https:ro

  nginx:
    image: nginx:latest
    container_name: nginx_load_balancer
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro  # Map the nginx.conf file
    depends_on:
      - rinhabackend
      - rinhabackend2
    networks:
      - api_network
    ports:
      - 9999:9999

  db:
    image: postgres:latest
    container_name: postgres_db
    ports:
      - "5432:5432"
    networks:
      - api_network
    environment:
      - POSTGRES_USER=rinhabackend
      - POSTGRES_PASSWORD=rinhabackend
      - POSTGRES_DB=rinhabackend
    command: ["postgres", "-c", "max_connections=300"]

  cache:
    image: redis:latest
    networks:
      - api_network
    ports:
      - '6379:6379' 

networks:
  api_network:
    driver: bridge
